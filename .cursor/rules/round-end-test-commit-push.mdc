---
description: 每轮改动结束后自动运行测试、提交、推送
alwaysApply: true
---

# 每轮结束：测试 → 提交 → 推送

完成一轮代码改动（例如实现某个 feature、修 bug、重构）后，**必须**按顺序执行：

1. **运行测试**
   - 执行：`uv run pytest tests/ -q --tb=line`
   - 若测试失败：先修复再重新跑测，直到全部通过。

2. **提交**
   - 只 stage 与本次改动相关的**源码与测试文件**（`src/**/*.py`、`tests/**/*.py` 等）。
   - **不要**把生成物加入提交（例如根目录下的 `weekly_report_mandate_*.json`）。
   - 使用清晰的 commit message（例如 `feat(scope): 简短描述` 或 `fix: 问题描述`）。

3. **推送**
   - 执行：`git push`（或当前分支对应的 remote 分支）。

若用户明确说「不要提交」「只跑测不推送」等，则按用户指示执行，不执行提交/推送。
