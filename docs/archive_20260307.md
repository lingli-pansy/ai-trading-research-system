# REFLECT 归档 — 2026-03-07

本文档为本次阶段工作的归档摘要，便于后续接续或复盘。

---

## 1. 本阶段完成内容

### MVP 与实盘前

- **MVP 5 条**：已达成；暂缓项改为「实盘前补齐」，见 [mvp_plan.md](mvp_plan.md)、[live_readiness_checklist.md](live_readiness_checklist.md)。
- **实盘前 L1–L7**：全部实现并文档化  
  - L1 多时间窗口回测：`run_backtest_windows.py`  
  - L2 OOS：`--oos-days N`  
  - L3/L4 回测阈值：`MIN_TRADE_COUNT`、`MAX_DRAWDOWN_PCT`  
  - L5 IBKR Paper：`ib_insync` + `execution/ibkr_client.py`，`run_paper` 在配置 IBKR_* 时走 TWS 下单  
  - L6 Kill Switch：`STOP_PAPER` / `.paper_stop`  
  - L7 券商连通：`verify_ibkr.py`

### LLM 与 Kimi

- Kimi Code 使用 **k2p5**、端点 `api.kimi.com/coding/v1`，与 OpenClaw 的 kimi-coding/k2p5 对齐。
- 通过 **User-Agent: KimiCLI/1.3** 过白名单，解决 403；可选 `KIMI_USER_AGENT`。

### 风控

- 仓位上限、单日止损实装于 `PaperRunner`；`PAPER_MAX_POSITION_PCT`、`PAPER_DAILY_STOP_LOSS_PCT` 可配置。

### 文档与仓库

- 删除 **plan_week_to_paper.md**，引用统一到 mvp_plan + live_readiness_checklist。
- **.gitignore**：补充 `.env.test`、`.experience/`、`.backtest_catalog/`、`.paper_stop`。
- **requirements.txt** 与 pyproject.toml 对齐（含 openai、ib_insync）。
- 本地已 **git commit**；远程为 `git@github.com:lingli-pansy/ai-trading-research-system.git`（SSH）。

### 本机环境

- **SSH**：已生成 `~/.ssh/id_ed25519_github`，并配置 `~/.ssh/config` 指向 GitHub。  
- **推送**：需在 GitHub [Settings → SSH keys](https://github.com/settings/keys) 添加公钥（lingli-pansy 账号）后执行 `git push -u origin main`。

---

## 2. 关键决策与修复

| 项 | 决策/修复 |
|----|-----------|
| Kimi 403 | 请求头加 `User-Agent: KimiCLI/1.3`（参考 CLIProxyAPI #1280） |
| Kimi 模型 | 默认 `k2p5`，与 OpenClaw kimi-coding/k2p5 一致 |
| IBKR 10349 | 市价单显式 `tif="DAY"` |
| ib_insync 事件循环 | 连接/下单放入 `asyncio.run(_async(...))` |
| IBKR 订单状态 | 用 `placed` 表示「已接单」，PendingSubmit 视为成功 |
| datetime 弃用 | `utcnow()` 改为 `datetime.now(timezone.utc)` |
| 文档入口 | 以 mvp_plan + live_readiness_checklist 为主，删除 plan_week_to_paper |

---

## 3. 当前状态

- **代码**：已提交（commit 91d75a4），49 文件变更。
- **推送**：待将公钥加入 GitHub 后执行 `git push -u origin main`。
- **实盘前检查项**：L1–L7 交付物就绪；检查项 5（IBKR Paper）、7（券商连通）在本地已验证，可按清单勾选。

---

## 4. 后续可接续

- 在 GitHub 添加 SSH 公钥并执行 `git push -u origin main`。
- 按 [live_readiness_checklist.md](live_readiness_checklist.md) 逐项勾选验收（含多窗口回测、OOS 阈值等）。
- OpenClaw 服务端/生产对接、生产密钥流程按 [deferred_authorization.md](deferred_authorization.md) 在授权后对接。

---

*归档时间：2026-03-07*
