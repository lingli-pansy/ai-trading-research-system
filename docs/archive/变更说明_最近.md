# 最近变更说明

## 0e28f68 — feat(UC-09): 周报纳入 Research 分析/新闻/盘面

- **Research**：`orchestrator.run_with_context(symbol)` 返回 `(ResearchContext, DecisionContract)`，便于上报新闻与盘面。
- **周报**：`WeeklyReport` 增加 `daily_research` 数组，每轮含 thesis、suggested_action、confidence、key_drivers、news_snippets、price_summary、fundamentals_summary。
- **Pipeline**：每轮收集上述内容写入周报 JSON；Experience Store 的 `extra` 写入 thesis/news_snippets/price_summary。
- **输出**：stdout summary 增加 `daily_research_count`、`analysis_in_report`；文档说明分析/新闻/盘面见 `report_path` 内 `daily_research`。

---

## d1a7384 — feat(UC-09): Weekly Autonomous Paper 闭环与文档收敛

- 自治层：AccountSnapshot、WeeklyTradingMandate、PortfolioAllocator、StateMachine、BenchmarkComparator、WeeklyReportGenerator。
- 入口：`cli weekly-paper`、`run_weekly_autonomous_paper.py`、OpenClaw `weekly_autonomous_paper`。
- 编排：weekly_paper_pipe 接入 research/nautilus/Experience Store；验证脚本 verify_uc09_weekly_autonomous_paper.py。
- 文档：current_state、openclaw_integration、result_schema、UC09_IMPLEMENTATION_PLAN；.gitignore 忽略 reports/。
